<Page class="page" navigatingTo="loaded"
    xmlns="http://schemas.nativescript.org/tns.xsd">
    <ActionBar class="action-bar">
        <Label class="action-bar-title" background-color="green" text="Detalles"></Label>
    </ActionBar>
    <GridLayout rows="50, 50, 50, 50, 50, 50, 50" columns="*, *">
            <Label text="ID" col="0" row="0" class="detail"/>
            <Label text="{{id}}" id="id" col="1" row="0" class="detail"/>
            <Label text="Nombre" col="0" row="1" class="detail"/>
            <Label text="{{nombre}}" col="1" row="1" class="detail"/>
            <Label text="Apellido" col="0" row="2" class="detail"/>
            <Label text="{{apellido}}" col="1" row="2" class="detail"/>
            <Label text="Acción" col="0" row="3" class="detail"/>
            <Label text="{{accion}}" id="accion" col="1" row="3" class="detail"/>
    </GridLayout>
</Page>